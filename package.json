{
  "name": "tezos-multisig",
  "version": "0.1.0",
  "description": "Miltisig implementation on Ligo",
  "dependencies": {
    "@taquito/signer": "^7.1.0-beta.0",
    "@taquito/taquito": "^7.1.0-beta.0",
    "bignumber.js": "^9.0.1",
    "truffle": "^5.5.0-tezos.4"
  },
  "devDependencies": {
    "ganache-cli": "6.11.0-tezos.0",
    "@types/chai": "^4.2.13",
    "@types/mocha": "^8.0.3",
    "chai": "^4.2.0",
    "mocha": "^7.2.0",
    "ts-node": "^9.0.0"
  },
  "scripts": {
    "start-sandbox": "ganache-cli --flavor tezos --seed alice",
    "clean": "rm -rf ./build",
    "migrate": "truffle migrate",
    "test": "tsc && truffle test"
  }
}
